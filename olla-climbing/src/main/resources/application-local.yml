# 개발용 설정

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/olla_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8 
    username: root 
    password: qaz13501 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 로컬에서는 테이블 자동 수정 허용
    properties:
      hibernate:
        format_sql: true
    show-sql: true